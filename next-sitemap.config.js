/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://www.mohammademamiproject.ir/',
  generateRobotsTxt: true,
  sitemapSize: 5000,
  changefreq: 'weekly',
  priority: 1.0,
};

module.exports = config;
